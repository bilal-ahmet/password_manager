import 'package:flutter/material.dart';

ThemeData darkTheme = ThemeData(
  colorScheme: const ColorScheme.light(
    // arka plan
    surface: Color(0xFF353238),
    // mail password
    primary: Color(0xFFB3A394),
    // sign in sign up button
    //#534D41
    secondary: Color(0xFF41393E),
    // appbar
    tertiary: Color(0xFF423E37)
  )
);